== Up & Running

* copy `config/application.yml.example` to `config/application.yml`, generate the secret token with `rake secret` and paste the result in the `config/application.yml`

* start the webrick with `rails s`

* open http://127.0.0.1:3000 with your favorite browser, that's all
